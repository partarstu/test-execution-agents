# Smoke tests share process-global state (the BudgetManager singleton and a static WireMock server),
# so the run must stay single-threaded. This is the JUnit Platform default, pinned here to keep an
# accidental opt-in to parallel execution from making the smoke suite flaky.
junit.jupiter.execution.parallel.enabled=false
